services:
  backend:
    build: .
    container_name: real-render-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
    volumes:
      - ./data:/app/data
